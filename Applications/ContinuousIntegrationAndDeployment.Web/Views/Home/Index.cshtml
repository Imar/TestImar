@model List<PersonDto>
@{
  ViewData["Title"] = "Home Page";
}
<div class="text-center">
  <h1 class="display-4">Welcome</h1>
  @foreach (var person in Model)
  {
    @person.FirstName @person.LastName @person.DateOfBirth<br />
  }
</div>
